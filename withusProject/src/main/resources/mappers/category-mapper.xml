<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- 6/29 윤경 생성 -->

<mapper namespace="categoryMapper">
	<resultMap id="category" type="Category">
		<result column="cat_no" property="catNo" />
		<result column="cat_name" property="catName" />
		<result column="cat_tag" property="catTag" />
		<result column="cat_file" property="catFile" />
	</resultMap>
	
	<select id="menuList" resultType="Category">
		select
		       cat_no
		     , cat_name
		     , cat_tag
		  from category
	</select>
	
	<insert id="insertCate" parameterType="Category">
		insert
		  into category
		     (
		       cat_no
		     , cat_name
		     , cat_tag
		     , cat_file
		     )
		 value
		     (
		       cno.nextval
		     , #{catName}
		     , #{catTag}
		     , #{catFile}
		     )
	</insert>
	
	<update id="updateCate" parameterType="Category">
		update
		       category
		   set cat_name = #{catName}
		     , cat_tag = #{catTag}
		 where cat_no = #{catNo} 
	</update>
	
	<delete id="deleteCate" parameterType="Category">
		delete
		  from category
		 where cat_name = #{catName}
	</delete>
	
</mapper>